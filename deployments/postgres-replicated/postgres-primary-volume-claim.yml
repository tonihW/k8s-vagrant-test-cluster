apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-primary-volume-claim
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
